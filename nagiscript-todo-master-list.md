# NagiScript 総合TODOリスト
## 修正が必要な項目 + 未実装の項目（v0.2〜v0.4設計仕様 × 実装ドキュメント 突き合わせ）

> チェックリスト形式です。優先度は「今の実装の一貫性に関わるか」「後回しにするとやり直しコストが大きいか」を基準にしています。

---

## A. 今すぐ直すべき仕様上の矛盾・バグ

これらは「まだ設計していない新機能」ではなく、**すでに書かれているドキュメント/コード同士が矛盾している**ものです。優先度が高い理由は、後から直すほど影響範囲（サンプルコード・stdの実装・利用者の理解）が広がるためです。

- [x] **`self`の値渡し/参照渡しのルールが未定義**：**参照渡しに決定**。メソッドの`self`は暗黙に参照として渡され、`self.n = ...`等のフィールド変更が呼び出し元に反映されることを実装（semaの代入先チェック修正でフィールド代入を可能化）し、`syntax.md`に記述・実証済み（`self`参照渡しで`incr`が呼び出し元へ反映）
- [x] **借用チェック(ライフタイムチェック)がsemaのアーキテクチャに残っている**：`compiler.md`内の`check_borrow`/`BorrowError`・「借用チェック」記述（実コードには存在しない）を削除し、**RC方式・借用チェッカーなし**の設計方針へ一本化
- [x] **`val`変数に対するミューテーティングメソッド呼び出しの扱いが不統一**：**`val`=バインディング再代入のみ禁止、中身（フィールド/List等）は可変**に決定。`keywords.md`/`syntax.md`を明文化して統一（`standard-library.md`の`val list`+`add`例はこの方針に整合）
- [x] **`i64`→`i32`の暗黙変換を「安全な縮小」と説明している**：縮小変換を**`as`必須**に変更（sema: `check_expr_expected`にビット幅縮小時のエラーを追加）。`types.md`/`basics.md`の記述も「`as`必須」に修正
- [x] **アドレス取得演算子の表記が資料内で割れている**：**単一`&`に統一**（レキサに`Amp`トークン追加、`&&`は論理ANDのまま、パーサのunaryアドレス取得を`&`に変更。`unsafe_demo.ngs`等も`&x`に修正）
- [x] **レキサの`IntLit`トークンが`f64`型で定義されている**：実装は既に`u64`で保持。`compiler.md`の`IntLit(f64)`記述を`IntLit(i64)`へ修正（精度保持の意図に整合）
- [x] **`async`キーワードの位置が資料内で不統一**：`async fn ...`形式に統一（`standard-library.md`の`fn main() async void`を`async fn main() void`へ修正）
- [x] **`!`と`not`の重複**：**`!`に統一**（実装は`!`のみで`not`は非対応）。`keywords.md`/`operators.md`の`not`記述を`!`へ修正
- [x] **`@doc {}`ブロック形式のドキュメントコメントの妥当性確認**：`@doc {}`は**未実装**（`@`は属性専用で`@repr(C)`のみ対応、`@doc`は「unknown attribute」エラー）。書きやすさ重視に合わせドキュメントコメントは`//`（または`/* */`）で記載する方針に変更し、`syntax.md`を修正

---

## B. 未実装：言語コア機能

- [ ] **双方向型検査(Bidirectional Type Checking)**：現状の`check_expr`は素朴な等価性チェック（`left_ty != right_ty`で即エラー）で、v0.3 3.2節で設計したcheck/synthesizeモードの使い分けはまだ形跡なし
- [ ] **クエリベース・インクリメンタルコンパイル(Salsaパターン)**：関数単位の型検査結果キャッシュは未実装。v0.3 3.3節で「Stage 1〜3で最初から組み込むべき」としていた前提事項
- [ ] **型推論の探索深さへのガードレール**：深いネストしたジェネリクスでの計算量爆発を防ぐ静的上限（v0.3 3.4節）は未実装
- [ ] **`.crsx`相当のJSX風UI構文**：`.ngs`内でReactライクなマークアップを直接書く構文（v0.2 4.4節、7.2節「モードB」）は未着手。現状は「Wasm化した関数をReact側からimportする」モードAのみ
- [ ] **LLVM IR/ビットコードレベルでの深い相互運用・LTO**：Rust/Zig/OdinのビットコードとのリンクタイムLTO統合（v0.2 6.2節「発展路線」）は未着手。C ABI境界（`extern fn`）のみ実装済み

---

## C. 未実装：並行処理

- [ ] **Go方式のスタックフル・グリーンスレッド（`spawn`/`chan`/`select`）**：v0.3 2.2節で設計した、関数の色分けが起きない並行処理モデル。現状はRust/JS方式の`async`/`await`のみ実装されている（4.1節で触れた設計判断の分岐点）
- [ ] **組み込み向けEmbassy方式のスタックレスコルーチン（`task fn`）**：v0.3 2.3節。マイコン向けのメモリ効率的な並行処理は未着手
- [ ] **M:Nワークスティーリングスケジューラ**：グリーンスレッドをOSスレッドに割り当てるランタイム本体

---

## D. 未実装：組み込み/ハードウェア

- [ ] **ESP32 RISC-V系（C3/C6/H2/P4）対応**：`microcontroller.md`では「準備中」表記のみ
- [ ] **ESP32 Xtensa系（無印/S2/S3）対応**：Espressifの独自LLVMフォームが必要（v0.3 1.1節）。RISC-V系より後回しが妥当
- [ ] **ARM Cortex-M対応**：`contributing.md`のロードマップ優先度1位だが未実装
- [ ] **Raspberry Piベアメタル（`aarch64-unknown-none`）**：OS自作用途、LFSプロジェクトとの接続（v0.3 1.2節）
- [ ] **`core::hal`的なハードウェア抽象化層**：GPIO/I2C/SPIの共通インターフェース（v0.3 1.1節）。現状は`microcontroller.md`にチップ固有の`extern fn`例があるのみ
- [ ] **UART/I2C/SPI/割り込み/パワーマネジメントの実機検証**：ドキュメント上はサンプルコードのみで、実際に動く実装かどうか不明

---

## E. 未実装：サーバー・クラウドネイティブ

- [ ] **TCP/UDPソケット、TLS**：v0.3 2.4節。現状のstdには`io`/`math`/`fs`/`args`/`async`モジュールのみで、ネットワーキング機能が見当たらない
- [ ] **HTTPサーバー/クライアント**：`async.md`に`http.get()`の使用例はあるが、`http`モジュール自体の定義がstandard-library.mdに存在しない（実装先行かドキュメント欠落か要確認）
- [ ] **JSONエンコード/デコード**：ドキュメント上に記載なし
- [ ] **musl静的リンクターゲット**（`x86_64-unknown-linux-musl`等）：Docker向けの軽量バイナリ生成（v0.4 1.1節）
- [ ] **SIGTERM/graceful shutdown対応**：Kubernetes Pod終了時の規約対応（v0.4 1.2節）
- [ ] **cgroups認識のランタイム調整**：コンテナのCPU/メモリ制限にワーカースレッド数を追従させる仕組み（v0.4 1.2節）
- [ ] **ヘルスチェック/レディネスプローブ用のユーティリティ**（v0.4 1.2節）
- [ ] **Kubernetes REST APIクライアントライブラリ**（v0.4 1.3節）

---

## F. 未実装：Linux GUI・ユーザーランド

- [ ] **wlroots/GTK/X11のFFIバインディング**：C ABI機構自体はあるが、GUIライブラリ向けの具体的なバインディングは未着手（v0.4 2.2節）
- [ ] **Cヘッダからのバインディング自動生成ツール（bindgen相当）**：`crs_driver`ならぬ`ngs_driver`への統合が未着手（v0.4 2.2節）
- [ ] **TOML/YAML設定ファイルパースの標準ライブラリ**：`project.toml`を読む機能はツール内部にあるはずだが、ユーザーコードから汎用的にパースするAPIとしては未確認
- [ ] **WonderLandプロジェクト（Zig製Waylandコンポジタ）との接続実証**：v0.4 2.2節で提案した「同じFFI手法が成立するかの実証」はまだ

---

## G. 未実装：ツールチェーン・エコシステム

- [ ] **統一パッケージマネージャ（ロックファイル＋ハッシュ検証）**：現状は`npm install -g nagiscript`と`cargo install ngs_driver`の2経路のみで、NagiScript独自のパッケージ管理（v0.4 3節のテーブルで挙げた「サプライチェーン対策」）は未着手
- [ ] **VSCode拡張機能**：`getting-started.md`に「準備中」と明記。TextMate/Tree-sitterグラマーは存在する模様
- [ ] **エラーメッセージの改善**：`contributing.md`のロードマップ優先度3。現状のエラー例（`Error[E001]`）は良い方向性だが「より親切に」は継続課題
- [ ] **コンパイル速度の改善**：`contributing.md`の優先度4。ベンチマーク・計測はまだ行われていない模様（v0.3 3.3節のインクリメンタルコンパイルとも関連）
- [ ] **チュートリアル・ドキュメントの拡充**：`contributing.md`の優先度5
- [ ] **セルフホスティング（NagiScript自身でコンパイラを書き直す）**：v0.2 Stage 7/8の長期目標。現状はRust実装のまま

---

## H. 確認・意思決定が必要な設計判断（実装の前に決めておきたいこと）

- [ ] **並行処理モデル**：async/await方式のまま進めるか、Go方式のspawn/chanを追加するか（4.1節参照）
- [ ] **借用チェックの扱い**：RCベース一本化か、部分的な借用チェック導入か（A節参照）
- [ ] **組み込み対応の優先順位**：ARM Cortex-M先行か、ESP32 RISC-V系先行か（どちらでも大きな問題はないが、開発リソース配分のため一度決めておくと良い）
- [ ] **`val`の不変性の強さ**：バインディングのみ不変か、内部状態も含めて不変か（A節参照、standard-library.mdの例と直結）

---

## 進め方の提案

A節（矛盾・バグ）は実装量が少なく、かつ後から直すほど波及範囲が広がるため、**先にA節を潰してからB〜G節の新規実装に進む**のが効率的です。特に「`self`の値渡し/参照渡し」と「借用チェックの扱い」の2つは、他の多くの機能（List/Stackのメソッド全般、メモリモデル全体）の前提になっているため、この2つを最優先で決め切ることをおすすめします。
