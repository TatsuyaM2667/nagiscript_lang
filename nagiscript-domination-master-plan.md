# NagiScript 覇権戦略 完全統合版

> これまでの設計仕様（v0.2〜v0.4）、実装ドキュメントレビュー、TODOリスト、「覇権戦略」ドキュメントへのレビュー、そして2026年時点の実際の競合言語の動向を全部統合した、最も詳しい版です。

---

## 0. このドキュメントの位置付け

これまで作った6つのドキュメント（コア仕様v0.2、追加仕様v0.3/v0.4、実装レビュー、TODOリスト、覇権戦略レビュー）の内容を土台に、**今回新しく2026年時点の実際の競合言語の状況を調べた上で**、言語設計・コンパイラ品質・エコシステム・コミュニティ・持続可能性の全領域を一本にまとめています。長いですが、目次で必要な章だけ拾い読みできる構成にしています。

---

## 1. 現在地の整理

- 名前：NagiScript（旧PostScript→CoreScript）、拡張子`.ngs`
- 実装：Rust製、`crates/`以下にlexer/parser/sema/ir/codegen_llvm/codegen_wasm/driver/std
- できていること：C ABI相互運用、`val`/`var`、ジェネリクス（単型化のみ）、`Result`+`?`演算子、Wasm+`.d.ts`自動生成
- 未決着の土台：`self`の値渡し/参照渡し、借用チェックの扱い、並行処理モデル
- 未着手：組み込み実機対応、サーバー機能（HTTP/TLS/JSON）、Docker/K8s、Linux GUI、パッケージマネージャ、LSP、フォーマッタ

---

## 2. 2026年時点の競合状況（新規調査）

「覇権戦略」ドキュメントの歴史分析は的確でしたが、**過去の言語がどう勝ったかだけでなく、今まさに同じ土俵で戦っている相手が誰かも見ておく必要があります。** 調べた結果、システムプログラミング言語の「Rust/C++の次」というポジションは、今かなり混雑してきています。

### 2.1 Zig — 最も警戒すべき相手

Zigは2026年半ば時点でもまだ1.0未満（最新は0.16.0、2026年4月リリース）ですが、これは**意図的な遅延**です。Zig Software Foundationの立場は「今1.0にしたら何を後悔するか」を基準に判断する、というもので、Tier 1ターゲットで無効化されたテストがゼロになることを1.0の条件の一つに掲げています。1.0のラベルが企業採用を加速させることをリード開発者のAndrew Kelley自身が認めた上で、あえて急いでいません。

**特に重要なのが、Zig 0.16で導入された`std.Io`という「色付けなし非同期I/O」の設計です。** これは我々がこれまで議論してきた「Go方式のgoroutine vs Rust方式のasync/await」という二者択一そのものを覆す、第3の道です。詳細は3.2節で扱いますが、**NagiScriptの並行処理モデルを最終決定する前に、必ず研究すべき設計**だと思います。

### 2.2 Odin — 「機能を削ることの強さ」を証明している

Odinは2026年に入ってから記事・言及が増えており、「機能を足すことではなく、削ることに強みがある」という評判が広がっています。ゲーム開発・グラフィックス分野で公式のvendorバインディング（OpenGL, Vulkan, Direct3D, SDL, GLFW）を整備していることが支持されている大きな理由の一つです。これはNagiScriptが目指す「Zigに行きたいがエコシステムがない層」の受け皿に、Odin自身がすでになりつつあることを意味します。**「シンプルさ」だけでは差別化しきれない**、という警戒信号として受け止めるべきです。

### 2.3 Zeta — 最も近いポジションの新興言語

2025年11月に登場したばかりの`Zeta`という言語は、Rust・Zig・OCaml等に影響を受けた「安全性・並行性・高性能」を掲げるシステム言語で、MITライセンスです。**掲げているコンセプトがNagiScriptとかなり近く**、直接の競合になり得ます。まだ立ち上がったばかり（v0.3.0が2026年1月）なので、今のうちに差別化ポイント（Wasm-First設計、C/Rust/Zig/Odinとの深い相互運用、組み込みからサーバーまでの一気通貫）を明確にしておく価値があります。

### 2.4 Carbon・Mojo・Jai — ニッチが異なる隣人

- **Carbon**（Google）：C++との100%相互運用・段階的移行に特化。狙いがNagiScriptとは違う（C++の漸進的置き換え、NagiScriptはRust/C/Zig/Odinとの相互運用＋新規プロジェクト向け）ので直接の競合ではない
- **Mojo**（Modular）：Python系構文、AI/ML特化。2026年5月に1.0.0 beta1。狙いのレイヤーが違うので競合というより「別の勝ち筋」の参考になる
- **Jai**（Jonathan Blow）：ゲーム開発特化、2026年2月時点でもクローズドベータのまま。オープンなエコシステム競争にはまだ参加していない

### 2.5 この調査からの示唆

- 「シンプルなシステム言語」というポジション自体はすでに激戦区。**Wasm-First + TypeScript/React連携という組み合わせは、この中で今のところ空いている**（Zig/Odin/Zetaはいずれもここに本気で取り組んでいない）。ここが最大の差別化ポイントであり続ける
- Zigが「1.0を急がない」ことで信頼を積み上げている事実は、**NagiScriptも拙速なバージョン1.0やeditionコミットメントを急ぐべきではない**という前回の指摘を裏付けている
- Zetaの存在は「安全＋高性能＋並行性」というだけでは差別化にならないという警告。**具体的な相互運用性とエコシステム連携（C/Rust/Zig/Odin全部＋Wasm/React）まで踏み込んでいるのはNagiScriptだけ**、という立ち位置を明確に打ち出すべき

---

## 3. 言語設計を完成させる

### 3.1 土台修正（最優先・詳細）

前回のTODOリストA節の中でも、特に以下の2つは他のすべての機能の前提になっています。

**`self`の値渡し/参照渡し**：現実的な解決策は「`self`は常に暗黙の参照として渡す」というルールを明文化することです。Pythonのオブジェクトモデルに近く、実装コストも比較的低く、既存の`List<T>.push`等のサンプルコードとも矛盾しません。ただし、値型（`Point`のような小さな構造体）まで常に参照渡しにすると、意図せずコピーが起きない・パフォーマンス特性が予測しづらいという問題も出るので、**「値型は値渡し、Rc/heap系（List, Stack等）は参照渡し」という区別を型システムレベルで明示する**（例えば`mut self`のような注釈を導入する）のがより厳密です。今すぐ完璧を目指すより、まず「参照渡しに統一する」という単純なルールで動かしてから、必要に応じて注釈を足す方が現実的だと思います。

**借用チェックの扱い**：RCベースで押し通すことを推奨します。理由は2.1〜2.3節で見た通り、Zig/Odin/Zetaも含めて「シンプルさ」を売りにする言語が乱立している中で、借用チェッカーを追加することは**Rustとの差別化を自ら消すこと**になるからです。`compiler.md`の`check_borrow`関連のコードとコメントは、実装するかどうか関係なく、意図が固まるまでは「保留」であることをコメントに明記しておくと、コントリビュータが混乱しません。

### 3.2 並行処理モデルの再検討：Zigの`std.Io`方式を取り込む提案

これは今回の調査で得た一番大きな収穫です。詳しく説明します。

Zig 0.16の`std.Io`は、要するに「**I/Oと並行処理の実装を、Allocatorと全く同じように『パラメータとして注入する』**」という設計です。

```
// Zigの考え方（NagiScript風に書き直した概念コード）
fn main() {
    val io = ThreadPoolIo.new()   // main()で一度だけ実装を選ぶ
    run_app(io)
}

fn run_app(io: Io) {
    // 以降、この io を使う関数はすべて「色付けなし」
    val data = fetch(io, "https://example.com")
}

fn fetch(io: Io, url: str) Result<str, str> {
    // io.get(...) のような呼び出しは、
    // ThreadPoolIoでもEventedIoでも同じ書き方で動く
    io.get(url)
}
```

この設計の重要な点は、**`fetch`という関数のシグネチャに`async`も`await`も登場しない**ことです。それでいて、`main()`で渡す`io`の実装をスレッドプール版にするかイベントループ版にするかで、実行時の並行処理戦略を切り替えられます。これはRustの「asyncエコシステムの分裂」ともGoの「ランタイムがすべてを隠蔽してしまう不透明さ」とも違う、第3の答えです（ただし2026年7月時点でもまだEvented版はexperimentalで、成熟しているのはThreadPool版のみ、という限界も正直にあります）。

**これがNagiScriptにとって重要な理由**：v0.3で悩んでいた「サーバー向けGo方式のspawn/chan」と「組み込み向けEmbassy方式のtask fn」という2層構成は、**実装の異なる`Io`インターフェースを注入する**という1つの仕組みに統合できる可能性があります。

- ネイティブサーバー向け：`ThreadPoolIo`（OSスレッドプールベース、実装が比較的容易）
- 組み込み向け：`EmbassyIo`（スタックレスコルーチンベース、ヒープ不要）
- Wasm向け：`SingleThreadIo`（ブロッキングのみ、シングルスレッド前提）

いずれも同じ関数シグネチャ・同じ呼び出し方で動くので、**「関数の色分けが起きない」というGoの美点を、target横断で実現できます**。これは前回の私のレビューで指摘した「Wasmのシングルスレッド性を理由にgoroutineを諦めるのは論拠が違う」という問題も、根本から解消します（target側の`Io`実装が違うだけで、コード側は変わらないので）。

**注意点**：この設計は実装が決して簡単ではありません（Zig自身も何年もかけて`std.Io`にたどり着いています）。ただし、**最初から`ThreadPoolIo`だけをサポートして始める**（Evented/Embassy版は後回し）なら、現実的な第一歩として十分に価値があります。これはv0.3で提案したロードマップの「Stage 10：サーバー機能拡充」を置き換える、より筋の良い設計だと思います。

### 3.3 型システムの決定事項

- **`null`/`Option`の統一**：`null`は`unsafe`ブロック内・生ポインタ型限定にする（前回のレビュー3.1節、最優先で取り込むべき）
- **暗黙の縮小変換の禁止**：`i64`→`i32`等は`as`必須にする（安全性重視の方針と一貫させる）
- **トレイトのスコープを先に文書化する**：「関連型は持たない」「演算子オーバーロードはしない」「型パラメータ1つにつき境界は1つまで」等、拡張要求が来る前に線を引く
- **`IntLit`トークンの内部表現**：`f64`ではなく整数型で保持する

### 3.4 構文の統一

- **戻り値の`->`**：現状維持（`fn f() i32`）を推奨。Zig/Go/Odinという手本と一致しており、パーサー上も曖昧さはない
- **`str`か`string`か**：数値型（`i32`, `u8`）の命名規則との一貫性を優先するなら現状の`str`維持。初心者への間口を最優先するなら数値型含めて全部フルスペルにする（中途半端な変更は避ける）
- **論理演算子**：`and`/`or`/`not`の単語方式に統一（`&&`/`||`/`!`は使わない）。ただしポインタのアドレス取得演算子は`&`（単一）に統一し、`&&`とは明確に区別する
- **enum値の生成**：データを持たないバリアントは括弧なし（`Color.Red`）、データを持つバリアントのみ括弧あり、で統一

### 3.5 式ベース設計・パターンマッチ・文字列補間

- `if`式・`match`式：`basics.md`を見る限りすでに部分的に動いている。仕上げれば良い
- パターンマッチの強化（範囲パターン、デストラクチャリング、ガード条件、OR パターン、ネストパターン）：覇権戦略ドキュメントの提案通り。Rust経験者への訴求力が大きいので、優先度は中〜高
- 文字列補間`f"..."`：最優先級。実装コストの割に体感の改善が最大

---

## 4. コンパイラ品質

### 4.1 エラーメッセージ

覇権戦略ドキュメントで示されていた`= help:`付きのエラー例は方向性として正しいです。加えて提案したいのは、**エラーメッセージ自体をテストの対象にする**ことです（「このコードを渡したら、このメッセージが出る」というスナップショットテストを`tests/`に用意する）。エラーメッセージは一度書いたら終わりではなく、リファクタリングのたびに劣化しやすい部分なので、最初から回帰テストの対象にしておくと後々の品質が安定します。

### 4.2 コンパイル速度

数値目標（100行で100ms未満、等）を掲げること自体は良い習慣ですが、**社外に発表する数値は必ず実測値のみにする**というルールを最初に決めておくことをおすすめします（2.5節で見た通り、Zigのように「爆速」を謳って後で叩かれるケースは非常によくあるパターンです）。

### 4.3 双方向型検査＋インクリメンタルコンパイル

v0.3で設計した内容のままです。重要なのは、**この設計は`crs_sema`改め`ngs_sema`を書き直すタイミングでしか導入できない**という点です。既存の`check_expr`（単純な等価性チェック）から双方向型検査への移行は、後回しにすればするほどコストが上がります。

---

## 5. エコシステム構築

### 5.1 パッケージマネージャ（`nagi`）

覇権戦略ドキュメントの設計（`nagi init`/`nagi add`/`nagi build`/`nagi test`/`nagi publish`）はCargoを踏襲した堅実な設計です。付け加えるとすれば、**ロックファイルにハッシュ検証を必須にする**（v0.4で挙げたサプライチェーン対策）ことと、`edition`フィールドは今は入れない（2.1節・前回レビュー3.6節）ことです。

### 5.2 LSP

優先順位は「エラー表示→型ホバー→補完→定義ジャンプ→リネーム→フォーマット統合」の順が定石です（他言語のLSP実装でも大体この順で価値が出ます）。Rustエコシステムには`tower-lsp`のようなLSPサーバー実装を助けるクレートがあるので、車輪の再発明を避けられます。

### 5.3 フォーマッタ

`nagi fmt`は「議論を消す」という効果が本質なので、**最初のバージョンから設定オプションをほぼゼロにする**（gofmtが成功した理由そのもの）ことを強く推奨します。設定可能にすればするほど、コミュニティ内でスタイル論争が復活します。

### 5.4 テストフレームワーク

`#[test]`方式は妥当です。加えて、**`nagi test`の実行結果をCIで使いやすい形式（JUnit XML等）で出力できるようにしておく**と、GitHub Actions連携がスムーズになり、コントリビュータの体験が良くなります。

### 5.5 REPL

4.3節のインクリメンタルコンパイル基盤ができていると、REPLの実装コストは大きく下がります（1行評価するたびに全部再コンパイルする必要がなくなるため）。優先順位としては、双方向型検査＋インクリメンタルコンパイルの後に着手するのが効率的です。

### 5.6 ドキュメント・Playground

Playground（ブラウザで動く実行環境）は、既存のWasmターゲットがあればそれほど実装コストは高くありません。`nagiscript wasm`の出力をブラウザで動かす仕組みは`webassembly.md`にすでにある通りなので、そのままPlayground化できます。

---

## 6. ターゲット展開（v0.3・v0.4からの統合）

- **組み込み**：ESP32はRISC-V系（C3/C6/H2/P4）を先行、Xtensa系（無印/S2/S3）は後回し。ARM Cortex-Mも並行して進めて問題なし。`core::hal`的な抽象化層は早めに設計しておく
- **サーバー/クラウドネイティブ**：3.2節の`Io`インターフェース方式を土台に、TCP/TLS/HTTP/JSONのstd実装、musl静的リンク、SIGTERM対応、cgroup認識を積む
- **Linux GUI/ユーザーランド**：wlroots/GTKのC ABIバインディング、bindgen相当のツール整備。WonderLandプロジェクトとの接続実証を最初のマイルストーンにする
- **Wasm/React**：すでに一番進んでいる領域。`.ngsx`（覇権戦略ドキュメントの命名）によるReactコンポーネント直接記述は長期ビジョンとして残しつつ、まずは既存の「モードA（Wasm化した関数をimportする）」の完成度を上げる

---

## 7. コミュニティ・マーケティング戦略

### 7.1 ポジショニング（2026年の競合を踏まえて再定義）

覇権戦略ドキュメントのキャッチフレーズ「Rustの安全性を、Goの書きやすさで、TypeScriptのエコシステムへ」は良い出発点ですが、2026年の競合状況（Zeta・Odin・Zig）を踏まえると、**「Wasm-Firstで、C/Rust/Zig/Odinのどれとも深く相互運用できる」という部分こそが本当の差別化ポイント**です。キャッチフレーズにこの要素をもう少し前面に出すことを検討する価値があります。例：「システムからWebまで、Rust/Zig/Odinの資産をそのまま活かせる唯一の言語」

### 7.2 ローンチ戦略

Hacker News / Reddit投稿は、**プロダクトとして最低限動くものが揃ってから**（3.1節の土台修正＋文字列補間＋フォーマッタ程度）にすることをおすすめします。早すぎるローンチは「面白そうだけどまだ何もできない」という第一印象がついて回りやすく、後から挽回するのは初回ローンチより難しいです。

### 7.3 日本語コミュニティ

前回のレビュー通り、これは実際に強い一手だと思います。Zenn/Qiitaでの記事投稿に加えて、**日本語ドキュメントが充実していること自体を英語圏にもアピールする**（「日本語ネイティブドキュメントを持つ数少ないシステム言語」という切り口）のも面白いかもしれません。

### 7.4 持続可能なOSS運営

- OSSライセンス（MIT/Apache-2.0）を早めに明記する
- Good First Issueを常時ストックする
- ベンチマーク・性能主張は実測値のみ公開する（4.2節）
- RFCプロセスは、コミュニティが実際に育ってから導入すれば十分（今から重いプロセスを敷く必要はない）

---

## 8. 持続可能性とリスク管理

### 8.1 歴史が示す現実的なタイムライン

覚えておく価値のある事実です。

| 言語 | 開始 | 1.0まで | 体制 |
|------|------|--------|------|
| Go | 2007年設計開始 | 2012年（5年） | Google、専任チーム |
| Rust | 2006年個人開発開始、2009年Mozilla支援 | 2015年（9年） | 企業支援あり |
| Zig | 2016年 | **2026年時点でまだ未到達（10年経過）** | 寄付ベースの非営利団体、意図的に急がない方針 |

**個人開発でこのペースを大幅に超えることは基本的にできません。** これは悲観ではなく、「今すぐ全部を完成させる必要はない」という前提を持つことが、燃え尽きを避けて長く続けるための一番の防御策だという話です。Zigが10年かけてなお1.0を急がないという判断を尊重されているように、**急がないこと自体が信頼につながる**、という戦い方もあります。

### 8.2 一人開発のリスク管理

- **「捨てる勇気」を繰り返し確認する**：これまでの全ドキュメントを通じて一番繰り返してきたメッセージです。組み込み・サーバー・GUI・Wasm・トレイト・パターンマッチ・並行処理モデル刷新——全部同時に進めようとすると、どれも中途半端になります
- **ドキュメントをコントリビュータ獲得の投資と捉える**：現状の`docs/`はすでにかなり丁寧に書かれています。これは将来コントリビュータが参加する際の障壁を大きく下げる資産です
- **バス係数（bus factor）を意識する**：一人開発である以上、設計判断の理由（なぜRCベースにしたか、なぜ`->`を使わないか等）を、コードコメントやREADMEに残しておくことが、将来コントリビュータが増えたときに「なぜこうなっているか分からない」という事態を防ぎます

---

## 9. 統合ロードマップ

### Phase 0（着手前に決め切る、1〜2週間）
- `self`の値渡し/参照渡しルールの明文化
- 借用チェックをやらない、という方針をコード上にも明記
- 並行処理モデルを`Io`インターフェース方式（3.2節）で行くかどうかの判断

### Phase 1（Month 1〜3、基盤修正）
- Phase 0の決定を反映した実装
- `null`/`Option`統一、暗黙縮小変換の禁止
- 文字列補間、if/match式の仕上げ
- `for`ループ・文字列連結など、既知の実装バグ修正
- 構文統一（アロー、演算子表記、enum構築）

### Phase 2（Month 4〜6、ツールチェーン）
- パッケージマネージャ`nagi`の基本機能
- テストフレームワーク、フォーマッタ、LSP基本機能
- エラーメッセージ改善（スナップショットテスト込み）
- `Io`インターフェースの`ThreadPoolIo`実装（3.2節）

### Phase 3（Month 7〜9、言語拡張）
- トレイト（スコープ明記の上で）
- パターンマッチ強化
- `try`ブロック、エラー型共通化
- REPL

### Phase 4（Month 10〜12、エコシステム＋ターゲット拡張）
- ESP32 RISC-V系対応
- musl静的リンク、Docker対応
- パッケージレジストリ、VS Code拡張公開
- 「NagiScript by Example」サイト、Playground拡充

### Phase 5以降（長期）
- ESP32 Xtensa系、Raspberry Piベアメタル
- Linux GUI（wlroots/GTKバインディング）
- Kubernetes APIクライアント
- `.ngsx`（React直接統合）
- セルフホスティング

---

## 10. まとめ：覇権に本当に必要な3つのこと

1. **土台を先に固める**：`self`の値渡し/参照渡し、借用チェックの扱い、並行処理モデル——この3つが決まらないまま新機能を積み上げると、いずれ全部作り直しになります
2. **差別化ポイントを絞り込む**：2026年の競合（Zig/Odin/Zeta）を踏まえると、「Wasm-First＋C/Rust/Zig/Odinとの深い相互運用」こそが空いているポジションです。ここに資源を集中させる
3. **急がないことを恐れない**：Zigが10年かけて1.0を急がず信頼を積み上げているように、一人開発のNagiScriptも「まだ荒削りだが誠実」という評判の方が、「派手だが壊れている」という評判より長期的に強いです
